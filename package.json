{
    "name": "git-split-diffs",
    "version": "0.17.0",
    "description": "Split diffs, now in your terminal",
    "homepage": "https://github.com/banga/git-split-diffs",
    "repository": "github:banga/git-split-diffs",
    "type": "module",
    "bin": "./build/index.mjs",
    "files": [
        "bin/*",
        "build/*.js",
        "themes/*.json"
    ],
    "license": "MIT",
    "engines": {
        "node": ">=18"
    },
    "keywords": [
        "git",
        "terminal",
        "shell",
        "diff",
        "diffs"
    ],
    "dependencies": {
        "ansi-regex": "^6.0.1",
        "chalk": "^5.3.0",
        "diff": "^5.1.0",
        "shikiji": "^0.9.13",
        "terminal-size": "^4.0.0",
        "wcwidth": "^1.0.1"
    },
    "scripts": {
        "clean": "rimraf './build'",
        "build": "yarn clean && yarn esbuild --external:benchmark --platform=node --target=node18 --bundle --format=esm --outdir=build --out-extension:.js=.mjs --banner:js='#!/usr/bin/env node'",
        "build:dev": "yarn build src/index.ts src/benchmark.ts src/previewTheme.ts",
        "build:publish": "yarn build src/index.ts --minify",
        "test": "yarn build:dev && NODE_OPTIONS='--experimental-vm-modules --no-warnings' yarn jest",
        "previewTheme": "nodemon -q build/previewTheme.js",
        "benchmark": "yarn build:dev && node build/benchmark.mjs",
        "prepublishOnly": "yarn test && yarn build:publish"
    },
    "devDependencies": {
        "@types/benchmark": "^2.1.0",
        "@types/diff": "^5.0.0",
        "@types/jest": "^29.5.1",
        "@types/node": "18.16.2",
        "@types/wcwidth": "^1.0.0",
        "benchmark": "^2.1.4",
        "esbuild": "^0.17.18",
        "esbuild-jest": "^0.5.0",
        "jest": "^29.7.0",
        "nodemon": "^2.0.7",
        "prettier": "^2.2.1",
        "rimraf": "^5.0.5",
        "ts-jest": "^29.1.0",
        "typescript": "^5.0.4"
    },
    "packageManager": "yarn@4.0.2",
    "resolutions": {
        "shikiji-core@npm:0.9.13": "patch:shikiji-core@npm%3A0.9.13#~/.yarn/patches/shikiji-core-npm-0.9.13-48559925ff.patch"
    }
}
