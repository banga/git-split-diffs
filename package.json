{
    "type": "module",
    "dependencies": {
        "@types/node": "^14.14.37",
        "ansi-regex": "^5.0.0",
        "chalk": "^4.1.0",
        "term-size": "^2.2.1",
        "typescript": "^4.2.3"
    },
    "scripts": {
        "clean": "rimraf ./build",
        "build": "yarn clean && tsc --build",
        "start": "yarn build && node ./build/index.js"
    },
    "devDependencies": {
        "@types/jest": "^26.0.22",
        "jest": "^26.6.3",
        "rimraf": "^3.0.2",
        "ts-jest": "^26.5.4"
    }
}
